;; -*- lexical-binding: t; -*-

(require 'expand-region)
(global-set-key (kbd "C-M-h") 'er/expand-region)
(setq expand-region-contract-fast-key "u")

(provide 'expand-region_init)
;;; expand-region_init.el ends here
