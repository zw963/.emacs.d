(require 'format-all)

;; (add-hook 'mint-mode-hook (lambda ()
;;                             (format-all-mode 1)
;;                             (local-set-key [(control c) (control c)] 'format-all-buffer)
;;                             ))

(provide 'format-all_init)

;;; format-all_init.el ends here
